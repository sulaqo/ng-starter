const PRODUCTION = 'prod';
const STAGING = 'stage';
const DEVELOPMENT = 'dev';

let allEnvironments = [PRODUCTION, STAGING, DEVELOPMENT];

export default {
  PRODUCTION,
  STAGING,
  DEVELOPMENT,
  allEnvironments
};
