import DoSomethingFactory from './do-something.service.factory';

describe('DoSomething', () => {
  var $mockedQ, $mockedTimeout, $mockedWindow;

  beforeEach(() => {
    $mockedWindow = {};
  });

  describe('it does what it is supposed to do', () => {
    it('should alert with something', () => {

    });
  });
});
